// Конфигурация роутов приложения

export default class RouterConfig {
  static MAIN = { path: '/' };

  static GAME = { path: 'game' };

  static AUTH = { path: 'signin' };

  static REGISTRATION = { path: 'registration' };

  static LOBBY = { path: 'lobby' };
}
